# Round 4 — `retrieve_for_ask` extraction (shipped)

**Status:** Shipped on `main` via [PR #40](https://github.com/alanmz-crypto/convmem/pull/40) @ `20fc85d` (partner PASS @ `2c7e599`).

Architecture / execution trail for parity-only `RetrievalBundle` + `retrieve_for_ask` in `ask.py`.
Stubs remain under [`../../planning/`](../../planning/) for old links.

**Follow-on shipped (no new debate round):** [PR #41](https://github.com/alanmz-crypto/convmem/pull/41) @ `da9f0f4` — Round 5 option B (supersession + site passthrough tests, WH golden rows). `eval-retrieval.py` still `query_units` only.

**Next arc (open):** retrieval-eval rewrite against `RetrievalBundle` / `bundle.trace`.
