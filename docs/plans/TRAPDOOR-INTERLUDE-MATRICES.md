# Arc Trapdoor Interlude Hunt — Claim, Evidence, and Trapdoor Bridge Matrices

```text
Status: Original FF1 accepted at 3c746faa47409f7def02d2fd24351fbc936a9720; original FF2 accepted at 0c2ab32b49a1a970fb3d1f76409d53ec1f0c6361; bounded T1-GEN/T1-REC/T1-BACK amendment accepted at 41d6af4e6e56797d65cbc23e52c40f4dc1795c94; Bridge refreshed against corrected T3 a46a3be2ed21252354dc0b18245059d342cfcb31; all VERIFY rows PENDING; implementation and Interlude lock unauthorized
Arc:    Arc Trapdoor Interlude Hunt
Base:   d10e1d5f4993f60a32142115f8b8c0f0f9ea4481
```

This file is the canonical matrix artifact. It is planning evidence, not a
runtime contract and not an implementation grant.

## Part A — FF1/T1 Trust Baseline claim matrix

Severity labels are accepted with FF1. No numerical operational target is
implied.

| Claim ID | Property | Scope | Acknowledgement boundary | Failure consequence | Severity | Owner | Oracle | Degraded state | Notes / assumptions |
|---|---|---|---|---|---|---|---|---|---|
| T1-ACK | Acknowledgement is emitted only after the named authoritative durable-write boundary has completed its declared durable commit; provider/index/projection/client/retrieval completion never substitutes for it. | Authoritative durable write and its recovery surface. | `file_generation_pointer.publish_active_pointer()` or a later explicitly named T3 authority-write boundary only. | Caller believes memory exists when authoritative state did not commit. | critical | Named acknowledgement boundary contract. | `tests/test_file_generation_pointer.py::test_postpublication_failure_requires_exact_durable_republish` | `unknown` | Supporting surfaces: `file_generation_pointer.py::recover_active_pointer`, `tests/test_file_generation_durability.py`, and `tests/test_atomic_files.py`. Unknown or failed writes do not report acknowledged success. The future T3 authority boundary must be named before implementation; registry publication remains an FF2 seed gap. The declared guarantee is durable commit at the named boundary; no undefined “failure class” variable and no `doctor PASS` substitution. |
| T1-GEN | An authority generation is internally complete and cannot silently mix generations; projection-backed serving activates only after required projections validate against that authority generation. | Authoritative registry/history, complete-data manifest, selected pointer, and separately activated JSONL/Chroma projections. | Authority recovery and projection-backed serving activation. | Recovery or serving combines incompatible state. | critical | Selected-generation publication contract. | T3 selected-generation authority/projection activation predicate (`V4i`–`V4k`, `V8l` jointly). | `BLOCKED` | Supporting surfaces: `file_generation_pointer.py::{publish_active_pointer,recover_active_pointer}` and `file_generation_validate.py::{run_cold_validation,cold_validate}`; supporting pointer, contract, and durability tests remain FF2 seed evidence. A valid authority may be `AUTHORITY_RECOVERED_PROJECTION_PENDING`; the generation-bound activation fence must block stale or mismatched projections and cannot fall back to the prior projection generation. Restore-side coherence remains distinct from capture-side coherence. |
| T1-CAP | A generation sealed as complete came from one consistent logical source state. | Capture/sealing cut across manifest-bound authoritative sources. | Generation sealing/publication boundary. | An impossible composite becomes authoritative despite individually valid parts. | critical | Capture/sealing consistency contract. | T3 `VERIFY-dependability-provenance.md::V4m` mutation-during-sealing negative control. | `quarantine` | Participating surfaces: `complete_data_restore.py::{writer_census_for_root,capture_backup_evidence}` and `restic_snapshot.py::backup_data_root`. Writer-census, complete-restore, and backup-workflow artifacts are provisional FF2 evidence only; the V4m oracle is planned and unimplemented, and no complete authority is published from an incoherent candidate. Maps to Trapdoor R8.2/V4m; freshness evidence alone is not a sealing proof. |
| T1-REC | Incomplete, unverifiable, or mismatched authority recovery fails closed; missing or damaged rebuildable projections enter a blocked projection-pending state rather than serving or invalidating otherwise sound authority. | Restore preflight, authority store, projections, recovery publication, and projection activation. | Recovered authority selection and projection-backed serving activation. | Corrupt or partial recovery is treated as healthy, or a stale projection serves against recovered authority. | critical | Recovered-authority publication contract. | T3 recovered-authority/projection-activation state predicate (`V4j`–`V4l`, `V8j`–`V8l` jointly). | `BLOCKED` | Supporting surfaces: `complete_data_restore.py::run_preflight_validation` and `backup_workflows.py::restore_validated_snapshot`; supporting complete-data, backup-workflow, and Chroma-restore tests are provisional FF2 evidence. Future registry-aware scratch restore and generation-bound activation fence remain unimplemented. Live authority remains unchanged on invalid authority recovery; projection-backed serving remains blocked while authority is pending. |
| T1-ID | Authoritative identity cannot be reconstructed merely from a projection or content equivalence. | Assertion identity and durable authority. | Identity-preserving import/replay boundary. | Projection or same-content injection aliases or overwrites an assertion. | critical | Assertion-authority contract. | T3 `VERIFY-dependability-provenance.md::V5k` invalid identity replay negative control. | `untrusted` | Existing T3 identity regime remains upstream design. Current ledger/Chroma identities are partial FF2 seed evidence; future Cursor P1 implementation remains separately Ryan-gated. T3 V5g–V5l are supporting planned checks; V5k is the single decisive oracle. |
| T1-TRANS | Transformations, exports, rebuilds, and restores cannot silently elevate provenance integrity. | Ingest, distill, inter-model, Chroma/export/reconstruction, CG-1, CG-2. | Current representation creation and publication. | Untrusted or unknown content becomes trusted through a pipeline step. | critical | Provenance transformation policy. | T3 `VERIFY-dependability-provenance.md::V8h` missing-ancestor recursive-verification negative control. | `untrusted` | Participating responsibility: the T3 provenance policy/transformer contract. Planned T3 V1–V4 and V8a–V8g checks are FF2 seed material; no current provenance envelope exists. Future Cursor P1/P2 implementation remains separately granted. Does not assert factual truth. |
| T1-SCHEMA | Unsupported future or malformed durable schemas fail conservatively. | Durable files, manifests, envelopes, and loaders. | Load/validate boundary. | New semantics are silently reinterpreted under an old reader. | critical | Durable-schema compatibility contract. | T3 `VERIFY-dependability-provenance.md::V1e` future-policy-version rejection fixture. | `unknown` | Supporting surfaces: `hash_schema_gate.py`, `tests/test_file_generation_contract.py`, and `tests/test_file_generation_validate.py`; planned T3 V1f/V4a fixtures and the future policy loader remain FF2 seed material. No migration operation belongs here. |
| T1-MIG | Migration cannot silently reinterpret durable meaning. | Schema migration and compatibility window. | Separately authorized migration write boundary. | Data changes meaning without approval or rollback. | critical | Migration compatibility contract. | T3 `VERIFY-dependability-provenance.md::V3i` migration-semantics contract check. | `needs migration` | T3 has a planning contract only. Existing schema-gate tests and the future separately granted migration owner are FF2 seed context; no implementation or live migration is authorized here. |
| T1-BACK | Backup and restore claims cover the authoritative state that matters and the complete-data-v2 recovery set, including rebuildable projections; authority recovery and projection-backed serving readiness are distinct. | Complete-data-v2 root, authority store, manifest, history, JSONL, Chroma, and activation fence. | Snapshot/capture, verified authority restore, and projection activation boundary. | A valid-looking backup omits the authority needed to trust recovery, or a stale projection serves against recovered authority. | critical | Complete-data backup contract. | T3 restored-authority completeness-and-coherence predicate (`V4g`–`V4l`, `V8i`–`V8l` jointly). | `quarantine` | Supporting surfaces: `backup_workflows.py::{ensure_current_snapshot,restore_validated_snapshot}` and `restic_snapshot.py`; supporting backup/offsite tests and snapshot path/tag/tree evidence are provisional FF2 evidence. A complete-data-v2 snapshot still normally captures registry, policy/recipe history, JSONL, and Chroma. Projection damage does not by itself invalidate verified authority, but it blocks projection-backed serving until generation-bound rebuild validation. The future application-level registry validator remains a gap; Restic evidence is not semantic provenance proof. |
| T1-PROVIDER | Provider or projection completion is not confused with durable acknowledgement. | LLM/provider, embedding, index, Chroma, and durable write orchestration. | Only authoritative durable boundary may acknowledge. | A transient/provider result is reported as durable memory. | high | Write orchestration contract at `observe.py::ingest_observation` and the governed `convmem.py` approval path. | Existing backup/write-gate tests and a future T3 V3h boundary trace; current cross-path coverage is incomplete. | unknown/pending; no acknowledgement. | Provider availability is not content truth. |
| T1-DEG | Missing authoritative state is visible as degraded/untrusted, not silently healthy. | Missing registry, stale history, unknown schema, incomplete ancestry, failed restore. | Every trust/authority decision using missing evidence. | Consumers act on an unproved state. | critical | Trust-state degradation policy. | T3 `VERIFY-dependability-provenance.md::V8j` missing/partial registry fail-closed check. | `unknown` | Supporting surfaces: `complete_data_restore.py::{worse_outcome,run_preflight_validation}` and `backup_workflows.py`; supporting complete-data and backup-workflow tests plus planned T3 V1/V8 missing-evidence controls are provisional FF2 evidence. State names must be concrete at each boundary. |

### FF1 acceptance checklist

- Ryan accepts or revises the severity vocabulary.
- Every `critical` claim has one owner, one oracle, and one degraded state.
- No claim relies on `doctor PASS` as its oracle.
- No numerical RPO/RTO/SLO or performance target has entered the matrix.
- Original FF1 acceptance is recorded at `3c746faa47409f7def02d2fd24351fbc936a9720`; Ryan accepted the bounded literature-driven T1-GEN/T1-REC/T1-BACK amendment at `41d6af4e6e56797d65cbc23e52c40f4dc1795c94`.

## Part B — FF2/T2 Existing Evidence + Failure-Gap Matrix

This is the completed FF2/T2 validation accepted at exact SHA
`0c2ab32b49a1a970fb3d1f76409d53ec1f0c6361`. The literature-driven bounded
reopening of T1-GEN, T1-REC, and T1-BACK was accepted at amendment revision
`41d6af4e6e56797d65cbc23e52c40f4dc1795c94`; the classifications below remain
unchanged. They are bounded planning adjudications against existing artifacts
only. They are not VERIFY results and do not authorize implementation,
migration, runtime, live-data, or operational work.

Aggregate classification: `9 PARTIAL`, `2 ABSENT`, `0 SUFFICIENT`, `0 STALE`.

| Claim ID | Existing evidence | Evidence revision / identity | Coverage / classification | Failure window covered | Evidence limit | Smallest missing oracle | Expected degraded state | Owner |
|---|---|---|---|---|---|---|---|---|
| T1-ACK | `file_generation_pointer.py::{publish_active_pointer,recover_active_pointer}`; `tests/test_file_generation_pointer.py::{test_prepublication_failure_leaves_n_serving,test_postpublication_failure_requires_exact_durable_republish}`; `tests/test_atomic_files.py::AtomicFilesTests::test_v5f_parent_dir_fsync_failure_after_replace`; `tests/test_file_generation_durability.py::test_process_crash_recovery_and_bar_p_claims_are_separate`. | Source/tests at `2f427fcfb8818dd665310bae7e8cd5ffa066bdcc` and `e2f8b0f40a7473cd4adc73c7c2b77749b830eb83`; FF1 tip `3c746faa47409f7def02d2fd24351fbc936a9720`. | PARTIAL | CG-1 pre-publication failure, post-replace durability uncertainty, and process-crash generation recovery. | No provenance-registry authority boundary or complete provider/index/projection/client-to-ack trace; the FF1 test oracle is not FF2 proof. | T3 `VERIFY-dependability-provenance.md::V3h` authoritative-write acknowledgement trace. | `unknown` | Named acknowledgement boundary contract |
| T1-GEN | `file_generation_pointer.py::{publish_active_pointer,recover_active_pointer}`; `file_generation_validate.py::{run_cold_validation,cold_validate}`; `tests/test_file_generation_validate.py::{test_revalidator_mutation_cannot_cross_final_cold_qualification,test_cross_wired_manifest_reference_cannot_select_another_owner,test_cold_process_validation_precedes_pointer_and_export_view_round_trips}`; `tests/test_file_generation_pointer.py::test_recovery_does_not_guess_when_manifest_or_rows_fail`. | Pointer tests at `2f427fcfb8818dd665310bae7e8cd5ffa066bdcc`; validator/tests at `e2f8b0f40a7473cd4adc73c7c2b77749b830eb83`; CG-2 VERIFY at `2f427fcfb8818dd665310bae7e8cd5ffa066bdcc`; FF1 tip `3c746faa47409f7def02d2fd24351fbc936a9720`. | PARTIAL | Candidate/pointer mismatch, fresh-process mutation, cross-wired manifest, and recovery corruption are rejected for current CG-1 surfaces. | The planned provenance registry, authority-recovery state, and generation-bound JSONL/Chroma activation fence are not evidenced. | T3 selected-generation authority/projection activation predicate (`V4i`–`V4k`, `V8l` jointly). | `BLOCKED` | Selected-generation publication contract |
| T1-CAP | `complete_data_restore.py::{writer_census_for_root,capture_backup_evidence}`; `tests/test_complete_data_restore.py::{TestTier1WriterCensusArtifact.test_inventory_file_and_hybrid_claims,TestEvidence.test_capture_writes_atomic_evidence,TestEvidence.test_evidence_comparison_mid_capture_skew_visible}`; `tests/test_restic_offsite.py::TestIntegratedHermeticBackupFlow.test_integrated_flow_capture_to_retained_reports`; `docs/plans/COMPLETE-DATA-V2-TIER1-WRITER-CENSUS.md`. | Source/tests/docs at `83b8c11683c1295579c4fad9c8316f9f8fc3d10f`; FF1 tip `3c746faa47409f7def02d2fd24351fbc936a9720`. | PARTIAL | Writer inventory, atomic capture-evidence publication, deliberately skewed evidence detection, and hermetic capture-to-snapshot flow. | No evidence proves every manifest-bound mutator is quiesced or that sealing cuts one consistent logical source state. | T3 `VERIFY-dependability-provenance.md::V4m` mutation-during-sealing negative control. | `quarantine` | Capture/sealing consistency contract |
| T1-REC | `complete_data_restore.py::{run_preflight_validation,inventory_restored_state}`; `backup_workflows.py::restore_validated_snapshot`; `tests/test_complete_data_restore.py::{TestBlockedAndSpecialCases.test_missing_required_collections_block,TestBlockedAndSpecialCases.test_unknown_top_level_blocks,TestBlockedAndSpecialCases.test_restore_drill_leakage_blocks,TestValidatorsNeverRepair.test_inventory_does_not_mutate_tree}`; `tests/test_backup_workflows.py::{TestBackupWorkflowsSWChallenge.test_restore_validated_snapshot_accepts_s_rejects_w,TestBackupWorkflowsSWChallenge.test_resolver_failure_never_false_greens}`; `tests/test_chroma_restore_drill.py::TestVerifyOnlyOpen.test_verify_open_refuses_missing_collection`. | Restore/backup source/tests at `83b8c11683c1295579c4fad9c8316f9f8fc3d10f`; Chroma restore test at `68b1634a2c0be539a436925c7fd6920267907290`; complete-data VERIFY at `dbc39d4ad5a8436f02357f82cef2ccf770a67849`; CG-2 VERIFY at `2f427fcfb8818dd665310bae7e8cd5ffa066bdcc`; FF1 tip `3c746faa47409f7def02d2fd24351fbc936a9720`. | PARTIAL | Missing required state/collections, unknown top-level state, restore-scope leakage, wrong-path selection, and validator non-repair behavior. | No provenance registry authority state or generation-bound projection activation is present or published by recovery. The older restore-drill artifact identified in the seed is `STALE` for current T3 proof and remains historical only. | T3 recovered-authority/projection-activation state predicate (`V4j`–`V4l`, `V8j`–`V8l` jointly). | `BLOCKED` | Recovered-authority publication contract |
| T1-ID | `ledger_ids.py::{observation_id,site_short,audit_key}`; `projection_parity.py::entity_key`; `tests/test_projection_parity.py::{test_ledger_identity_uses_ledger_id_instead_of_chroma_uuid,test_report_separates_active_claims_history_and_locked_ledger_gap}`; `VERIFY-cg2-production-activation.md` logical-accounting rows. | Ledger IDs at `305013bd28edeb23a1882085363e57d552460b98`; projection source/tests at `e88a3e3c65c13322c3e8804ccbea72dfe72b9a51`; CG-2 VERIFY at `2f427fcfb8818dd665310bae7e8cd5ffa066bdcc`; FF1 tip `3c746faa47409f7def02d2fd24351fbc936a9720`. | PARTIAL | Deterministic domain ledger IDs and projection comparisons distinguish durable `ledger_id` from a Chroma UUID in existing domains. | These identifiers are not monitor-minted provenance assertion authority and do not prove replay, parent commitment, or projection-alias resistance. | T3 `VERIFY-dependability-provenance.md::V5k` invalid identity replay negative control. | `untrusted` | Assertion-authority contract |
| T1-TRANS | `docs/plans/VERIFY-shadow-ledger-phase0.md`; `docs/plans/VERIFY-cg2-production-activation.md`; `projection_parity.py` and its logical-key tests document adjacent projection/serving/durability separations only. | Shadow VERIFY at `4535107143279c87e8b34c1eab7e4dee88bffc68`; CG-2 VERIFY/source at `2f427fcfb8818dd665310bae7e8cd5ffa066bdcc` and `e88a3e3c65c13322c3e8804ccbea72dfe72b9a51`; FF1 tip `3c746faa47409f7def02d2fd24351fbc936a9720`. | ABSENT | No claimed transformation/provenance failure window is covered; adjacent controls separate projection, serving, and durability claims but do not implement provenance integrity. | No canonical provenance envelope, policy calculation, transformer cap, or continuity implementation exists in the inspected revision; there is no direct evidence to classify this property as `PARTIAL`. | T3 `VERIFY-dependability-provenance.md::V8h` missing-ancestor recursive-verification negative control. | `untrusted` | Provenance transformation policy |
| T1-SCHEMA | `hash_schema_gate.py::{is_hashless_targeted,graduation_state,enforce_hashless_on_approve}`; `tests/test_hash_schema_gate.py::{test_warn_then_block_after_14_days,test_zero_hashless_graduates_to_block_for_new_hashless}`; `tests/test_file_generation_contract.py::test_manifest_and_pointer_hashes_fail_closed`; `tests/test_file_generation_validate.py::test_cold_validation_fails_after_corruption_in_fresh_process`. | Hash-schema source/tests at `f9a392768c47bf2e92f63ae7d26ad61ca981ecbf`; generation source/tests at `e2f8b0f40a7473cd4adc73c7c2b77749b830eb83`; FF1 tip `3c746faa47409f7def02d2fd24351fbc936a9720`. | PARTIAL | Existing hashless-targeted graduation and generation manifest/pointer hash mismatch paths reject or block known malformed state. | These gates do not cover provenance envelope, policy, transformer, recipe, or future-version semantics. | T3 `VERIFY-dependability-provenance.md::V1e` future-policy-version rejection fixture. | `unknown` | Durable-schema compatibility contract |
| T1-MIG | T3 `docs/plans/ARCHITECTURE-dependability-provenance.md` migration contract: N-1 window, reject-future, dry-run, backup-before-write, atomic replacement, and rollback. | Trapdoor T3 reference SHA `8f037a50c4cdce170320bbfd6160c932f7661798`; T3 `VERIFY-dependability-provenance.md::V3i` remains `PENDING`; FF1 tip `3c746faa47409f7def02d2fd24351fbc936a9720`. | ABSENT | Planning contract specifies the intended migration failure window and permission boundary only. | No migration implementation or behavioral evidence exists; V3i is a planning oracle, and no live migration is authorized. | T3 `VERIFY-dependability-provenance.md::V3i` migration-semantics contract check. | `needs migration` | Migration compatibility contract |
| T1-BACK | `backup_workflows.py::{ensure_current_snapshot,restore_validated_snapshot}`; `restic_snapshot.py`; `complete_data_restore.py::build_backup_evidence`; `tests/test_backup_workflows.py::{TestBackupWorkflowsSWChallenge.test_restore_validated_snapshot_accepts_s_rejects_w,TestBackupWorkflowsSWChallenge.test_resolver_failure_never_false_greens}`; `tests/test_restic_offsite.py::TestIntegratedHermeticBackupFlow.test_integrated_flow_capture_to_retained_reports`; complete-data VERIFY and writer-census plans. | Backup/restore source/tests at `83b8c11683c1295579c4fad9c8316f9f8fc3d10f`; complete-data VERIFY at `dbc39d4ad5a8436f02357f82cef2ccf770a67849`; writer census at `83b8c11683c1295579c4fad9c8316f9f8fc3d10f`; FF1 tip `3c746faa47409f7def02d2fd24351fbc936a9720`. | PARTIAL | Correct-path snapshot selection, tag/tree lineage, offsite original identity, restore preflight, and current durable-surface validation. | The future provenance registry, authority-first recovery state, and generation-bound projection activation are not evidenced; complete-data capture still does not prove semantic authority. | T3 restored-authority completeness-and-coherence predicate (`V4g`–`V4l`, `V8i`–`V8l` jointly); `V8i` is a constituent registry-presence check, not the complete oracle. | `quarantine` | Complete-data backup contract |
| T1-PROVIDER | `observe.py::ingest_observation`; `tests/test_upsert.py`; `tests/test_write_gate_effect.py`; `projection_parity.py` and `tests/test_projection_parity.py` distinguish write/projection paths in adjacent controls. | Observe/projection source/tests at `e88a3e3c65c13322c3e8804ccbea72dfe72b9a51`; write-gate test at `248ca2eeccc153328cd5d0c4ec681dc4cf082b04`; FF1 tip `3c746faa47409f7def02d2fd24351fbc936a9720`. | PARTIAL | Existing write-gate and projection-accounting paths preserve some distinction between durable writes and projection completion. | No complete provider-failure-to-acknowledgement trace covers every write path or proves the authoritative boundary. | `observe.py::ingest_observation` provider-to-acknowledgement boundary trace. | `unknown` | Write orchestration contract |
| T1-DEG | `complete_data_restore.py::{worse_outcome,run_preflight_validation,inventory_restored_state}`; `backup_workflows.py`; `tests/test_complete_data_restore.py::{TestBlockedAndSpecialCases.test_missing_required_collections_block,TestBlockedAndSpecialCases.test_unknown_top_level_blocks,TestOutcomePrecedence.test_blocked_beats_repairable}`; T3 names `provenance_store_unavailable`. | Restore source/tests at `83b8c11683c1295579c4fad9c8316f9f8fc3d10f`; Trapdoor T3 reference SHA `8f037a50c4cdce170320bbfd6160c932f7661798`; FF1 tip `3c746faa47409f7def02d2fd24351fbc936a9720`. | PARTIAL | Known restore classifiers block missing/unknown state and preserve stronger `BLOCKED` outcomes over repairable outcomes. | Cross-arc behavior for missing/partial provenance registry, ancestry, policy, and history is not implemented or evidenced; FF1 vocabulary alone is not runtime proof. | T3 `VERIFY-dependability-provenance.md::V8j` missing/partial registry fail-closed check. | `unknown` | Trust-state degradation policy |

### FF2 classification rules

`PARTIAL` does not mean “almost sufficient.” It means the evidence proves a
narrower claim and must not be cited for the unproved portion. The two older
restore-drill artifacts are `STALE` for current T3 proof where their subject
SHA is not current; they remain historical evidence only.

## Part C — Trapdoor Bridge

Bridge basis: accepted/amended Interlude matrix revision
`41d6af4e6e56797d65cbc23e52c40f4dc1795c94`; corrected Trapdoor T3 planning
revision `a46a3be2ed21252354dc0b18245059d342cfcb31`.

Bridge status: **REFRESHED against corrected T3 — awaiting Ryan's Interlude
lock.** This is a bounded traceability map, not proof that any T3 requirement
is implemented. Every VERIFY result remains `PENDING`; every FF2 gap and
classification is preserved. `MAPPED — gap preserved` means the path is
truthful but the missing oracle or implementation evidence remains missing.
`MAPPED — planning-only` means the T3 requirement is intentionally not being
executed in this Bridge.

| Accepted T1 claim | T2 evidence / gap | Existing T3 requirement | Existing T3 VERIFY row | Bridge disposition |
|---|---|---|---|---|
| T1-ACK | FF2 `PARTIAL`: current pointer/durability tests cover pre-publication, post-replace, and process-crash windows; the authoritative provenance-write boundary and complete provider/index/projection/client-to-ack trace remain missing. | T3 authoritative durable-write acknowledgement is conditional on a declared supported persistence profile; provider, projection, index, client, and retrieval completion cannot acknowledge. | `V3h` | **MAPPED — gap preserved.** The FF2 missing boundary oracle remains unimplemented; no acknowledgement claim is upgraded and no T3 write is authorized. |
| T1-GEN | FF2 `PARTIAL`: current generation/pointer/manifest checks reject current-surface mismatches and corruption; provenance registry binding and generation-bound projection activation are not evidenced. | T3 R8.1/R8.2 require one selected generation and authoritative registry/history/graph/continuity recovery; valid authority may enter `AUTHORITY_RECOVERED_PROJECTION_PENDING`, while serving requires generation/commitment-bound projections and no stale fallback. | `V4g`–`V4l`, `V8i`–`V8l` | **MAPPED — gap preserved.** The accepted amendment resolves authority recovery versus serving readiness without changing the FF2 classification; implementation evidence remains absent and VERIFY remains PENDING. |
| T1-CAP | FF2 `PARTIAL`: writer census, atomic capture-evidence publication, skew detection, and hermetic capture flow cover narrower windows; universal manifest-bound mutator participation and one consistent logical source state are not proved. | T3 R8.2/V4m require sealing from one consistent logical source state, with every manifest-bound mutator covered or all authoritative writers entering one immutable staging boundary. | `V4m` | **MAPPED — gap preserved.** V4m is a planned prerequisite oracle, not evidence supplied by the existing census; no capture implementation or broad campaign is authorized. |
| T1-REC | FF2 `PARTIAL`: existing restore/preflight checks fail closed for missing/unknown current surfaces, but provenance authority recovery and generation-bound projection activation are not evidenced. | T3 R8.1/R8.2 separate recovered authority from projection-backed serving: invalid authority leaves live authority unchanged; valid authority with damaged projections enters projection-pending; rebuild is from recovered authority and activation cannot use stale projections. | `V4i`–`V4l`, `V8i`–`V8l` | **MAPPED — gap preserved.** The accepted authority-first amendment is reflected without weakening fail-closed serving; the recovery/activation implementation and evidence remain missing. |
| T1-ID | FF2 `PARTIAL`: current ledger and projection identities distinguish durable ledger IDs from Chroma UUIDs, but do not establish monitor-minted provenance authority, replay validity, parent commitments, or alias resistance. | T3 R7 requires monitor-minted immutable assertion identity; R8 requires parent ID/expected commitment agreement and recursive fail-closed verification. Downstream action authority remains out of scope. | `V5g`–`V5l` (decisive: `V5k`) | **MAPPED — gap preserved.** Existing IDs are not promoted to provenance authority; the planned identity/replay controls remain PENDING and no identity architecture change is introduced. |
| T1-TRANS | FF2 `ABSENT`: adjacent projection/serving/durability controls do not implement provenance propagation, policy calculation, transformer caps, or continuity. | T3 R1–R5/R8 require conservative integrity meet/cap propagation, bound provider/recipe/transformer evidence, immutable artifact identity for `trusted` eligibility, and recursive verification before effective integrity. Under the current production ingress inventory, all real roots and descendants remain `untrusted`; V1b/V1h make Verified Ingress Bootstrap a prerequisite before `agent`/`trusted` become operational production capabilities, and R8 pins recursive verification to one immutable authority generation/snapshot. | `V1`–`V4`, `V8a`–`V8h` (specifically `V1b`, `V1h`, `V2h`, `V2i`; decisive failure control: `V8h`) | **MAPPED — gap preserved.** This remains an honest path from an ABSENT FF2 row to planned T3 requirements; no absent evidence is upgraded and no algebra, bootstrap design, or implementation is authorized. |
| T1-SCHEMA | FF2 `PARTIAL`: current hash/generation gates reject known malformed state, but provenance envelope, policy, transformer, recipe, and future-version semantics are not evidenced. | T3 versioned envelope/policy/recipe semantics reject future or unknown versions, use strict validated typed-envelope input before canonicalization, bind policy/recipe semantic bytes by digest, and identify schema/binding/canonicalization semantics with immutable `schema_semantics_sha256`; same-identifier/different-semantic-bytes substitution fails closed. | `V1e`–`V1f`, `V3i`, `V4a`, `V8k` | **MAPPED — gap preserved.** Current hashless-graduation evidence is not overextended into provenance-schema proof; the corrected schema semantic-digest requirement is now explicit, with no migration execution or canonicalization redesign authorized. |
| T1-MIG | FF2 `ABSENT`: T3 specifies N-1, reject-future, dry-run, backup-before-write, atomic replacement/rollback, and semantic mapping/fixture obligations, but no migration implementation or behavioral evidence exists. | T3 migration contract requires explicit old-to-new semantic mapping and schema-complete old-state fixture evidence for envelope, assertion ID, commitment, parent edges, and other meaning-bearing categories; unmapped/changed meaning fails closed. No live migration is allowed here. | `V3i` | **MAPPED — planning-only.** The contract is traceable, but the ABSENT classification remains ABSENT; this Bridge grants neither migration execution nor implementation. |
| T1-BACK | FF2 `PARTIAL`: current snapshot selection, lineage, restore preflight, and durable-surface checks cover existing surfaces; future registry authority and projection activation are not evidenced, and backup evidence is not semantic provenance proof. | T3 complete-data-v2 still captures registry/history plus JSONL/Chroma as the recovery set; the restored-authority completeness/coherence predicate validates authority and continuity, while projection damage blocks serving and requires generation-bound rebuild/activation. | `V4g`–`V4l`, `V8i`–`V8l` (joint predicate; `V8i` is constituent only) | **MAPPED — gap preserved.** The accepted amendment preserves complete-data coverage while separating authority recovery from serving readiness; no backup, restore, or projection operation is authorized. |
| T1-PROVIDER | FF2 `PARTIAL`: existing write-gate and projection-accounting paths preserve some durable-write/provider distinction, but no complete cross-path provider-failure-to-ack trace proves the authoritative boundary. | T3 acknowledgement remains exclusively at the authoritative durable boundary; provider, index, projection, client, or retrieval completion cannot elevate it. | `V3h`, `V8g` | **MAPPED — gap preserved.** The contractual distinction is retained and the missing trace remains a gap; no provider, projection, or runtime work is authorized. |
| T1-DEG | FF2 `PARTIAL`: current restore classifiers block missing/unknown state, but cross-boundary provenance registry, ancestry, policy, and history degradation is not implemented or evidenced. | T3 R8 and fail-closed trust-state rules require explicit `unknown`/`untrusted`/`quarantine`/`BLOCKED`/projection-pending outcomes for missing authority, ancestry, history, schema, or projection conditions; no degraded state may silently become healthy. | `V1b`–`V1f`, `V8h`–`V8l` (decisive missing-registry control: `V8j`) | **MAPPED — gap preserved.** Concrete states remain distinct and VERIFY remains PENDING; the Bridge does not adjudicate runtime behavior. |

### Bridge completion rule

The Bridge is complete as a planning traceability artifact when every accepted
T1 claim has an upstream FF2 evidence/gap disposition, an existing T3
requirement/invariant, and an existing T3 VERIFY row, with no row implying
runtime authorization. This revision refreshes the T1-TRANS and T1-SCHEMA
traceability against corrected T3 `a46a3be2ed21252354dc0b18245059d342cfcb31`;
FF1/FF2 claims and classifications remain unchanged. Ryan's next decision is
Interlude lock, not a new FF1/FF2 adjudication.

**TL;DR:** [Arc Trapdoor Interlude Hunt] The accepted FF1/FF2 claims now have a
truthful claim → evidence/gap → T3 requirement → VERIFY trace for all 11 rows;
the Bridge records gaps without granting implementation, and all VERIFY rows
remain PENDING.
