# Sourced by interactive shell: source ~/.config/convmem/env.local
# For systemd units, also maintain env.systemd (no export, no functions) — see SYSTEMD-DEPLOY.md

export DEEPSEEK_API_KEY=REPLACE_ME

convmem() {
  ~/miniforge3/envs/convmem/bin/python ~/Projects/convmem/convmem.py "$@"
}
