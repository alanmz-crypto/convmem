# convmem — session start (loads first: 00-*)

**STOP.** Before `ls`, git, docker, `stack_ps`, or reading project files:

```bash
convmem doctor && convmem brief --stdout-only && convmem unresolved
```

DeepSeek V4 (Flash/Pro): do not skip this on "project state" questions. Full protocol: `rules/convmem.md`.
